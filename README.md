# 🧾 Basic Sales Summary using Python & SQLite

## 🎯 Objective
A simple Python project to practice **SQL inside Python** — retrieving sales information such as **total quantity sold** and **total revenue** — and displaying it with a basic **bar chart**.

---

## 🛠️ Tools Used
- **Python 3.x**
- **SQLite3** (built into Python — no setup required)
- **Pandas**
- **Matplotlib**
- **VS Code or Jupyter Notebook**

---

## 📂 Project Files
| File | Description |
|------|--------------|
| `sales_summary.py` | Main Python script that creates the database, runs SQL queries, and generates the sales chart. |
| `sales_data.db` | SQLite database containing sample sales data. |
| `README.md` | Project documentation. |

---

## 🚀 Steps to Run the Project

### 1️⃣ Install Dependencies
Open a terminal in your project folder and run:
```bash
pip install pandas matplotlib
